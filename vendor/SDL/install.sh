brew install sdl2 SDL2_ttf SDL2_image

# echo getting $1 https://www.libsdl.org/$2release/$1.tar.gz
# curl -OL https://www.libsdl.org/$2release/$1.tar.gz
# echo untaring
# tar xf $1.tar.gz
# echo installing.
# cd $1/ && SDL2_CONFIG=$(pwd)/../bin/sdl2-config ./configure --prefix=$(pwd)/../ && make && make install
# cd ../ && rm $1.tar.gz
