LIBFT_PATH=../../libft
BRACKETS_INCLUDE += $(addprefix -I, $(LIBFT_PATH) ../objects)
BRACKETS_SRCS = $(addprefix srcs/, parser.c  configure.c lexer.c  parse_utils.c check.c $(addprefix types/, data.c globals.c))
